/* file: enclose_pregion_in_cregion.swift 	 generated: Thu Mar  5 17:54:18 2026 */

/* This file was generated by the EXPRESS to Swift translator "exp2swift", 
  derived from STEPcode (formerly NIST's SCL).
 exp2swift version: v.2.1.0, derived from stepcode v0.8 as of 2019/11/23 
 WARNING: You probably don't want to edit it since your modifications 
  will be lost if exp2swift is used to regenerate it.
 */

 
import SwiftSDAIcore

extension AP242_MANAGED_MODEL_BASED_3D_ENGINEERING_MIM_LF {
//MARK: -FUNCTION definition in EXPRESS

/** FUNCTION definition
- EXPRESS source code:
```express

  FUNCTION enclose_pregion_in_cregion(
               prgn : polar_complex_number_region
           ) : cartesian_complex_number_region;

    PROCEDURE nearest_good_direction(
                  acart : REAL;
                  aitv : finite_real_interval;
                  VAR a : REAL;
                  VAR a_in : BOOLEAN      );
      a := acart;
      a_in := TRUE;
      IF a < aitv.min THEN
        IF ( a + ( 2 * PI ) ) < aitv.max THEN
          RETURN;
        END_IF;
        IF ( a + ( 2 * PI ) ) = aitv.max THEN
          a_in := max_included( aitv );
          RETURN;
        END_IF;
      ELSE
        IF a = aitv.min THEN
          a_in := min_included( aitv );
          RETURN;
        ELSE
          IF a < aitv.max THEN
            RETURN;
          ELSE
            IF a = aitv.max THEN
              a_in := max_included( aitv );
              RETURN;
            END_IF;
          END_IF;
        END_IF;
      END_IF;
      IF COS( acart - aitv.max ) >= COS( acart - aitv.min ) THEN
        a := aitv.max;
        a_in := max_included( aitv );
      ELSE
        a := aitv.min;
        a_in := min_included( aitv );
      END_IF;

    END_PROCEDURE; -- nearest_good_direction (line:39579 file:ap242ed2_mim_lf_v1.101_TY.exp)

    LOCAL
      xc          : REAL := 0;
      yc          : REAL := 0;
      xmin        : REAL := 0;
      xmax        : REAL := 0;
      ymin        : REAL := 0;
      ymax        : REAL := 0;
      ritv        : real_interval;
      xitv        : real_interval;
      yitv        : real_interval;
      aitv        : finite_real_interval;
      xmin_exists : BOOLEAN;
      xmax_exists : BOOLEAN;
      ymin_exists : BOOLEAN;
      ymax_exists : BOOLEAN;
      xmin_in     : BOOLEAN := FALSE;
      xmax_in     : BOOLEAN := FALSE;
      ymin_in     : BOOLEAN := FALSE;
      ymax_in     : BOOLEAN := FALSE;
      a           : REAL := 0;
      r           : REAL := 0;
      a_in        : BOOLEAN := FALSE;
      min_clo     : open_closed := open;
      max_clo     : open_closed := open;
    END_LOCAL;
    IF NOT EXISTS( prgn ) THEN
      RETURN( ? );
    END_IF;
    xc := prgn.centre.real_part;
    yc := prgn.centre.imag_part;
    ritv := prgn.distance_constraint;
    aitv := prgn.direction_constraint;
    nearest_good_direction( PI, aitv, a, a_in );
    IF COS( a ) >= 0 THEN
      xmin_exists := TRUE;
      xmin := xc + ( real_min( ritv ) * COS( a ) );
      xmin_in := a_in AND ( min_included( ritv ) OR ( COS( a ) = 0 ) );
    ELSE
      IF max_exists( ritv ) THEN
        xmin_exists := TRUE;
        xmin := xc + ( real_max( ritv ) * COS( a ) );
        xmin_in := a_in AND max_included( ritv );
      ELSE
        xmin_exists := FALSE;
      END_IF;
    END_IF;
    nearest_good_direction( 0, aitv, a, a_in );
    IF COS( a ) <= 0 THEN
      xmax_exists := TRUE;
      xmax := xc + ( real_min( ritv ) * COS( a ) );
      xmax_in := a_in AND ( min_included( ritv ) OR ( COS( a ) = 0 ) );
    ELSE
      IF max_exists( ritv ) THEN
        xmax_exists := TRUE;
        xmax := xc + ( real_max( ritv ) * COS( a ) );
        xmax_in := a_in AND max_included( ritv );
      ELSE
        xmax_exists := FALSE;
      END_IF;
    END_IF;
    nearest_good_direction( ( -0.5 ) * PI, aitv, a, a_in );
    IF SIN( a ) >= 0 THEN
      ymin_exists := TRUE;
      ymin := yc + ( real_min( ritv ) * SIN( a ) );
      ymin_in := a_in AND ( min_included( ritv ) OR ( SIN( a ) = 0 ) );
    ELSE
      IF max_exists( ritv ) THEN
        ymin_exists := TRUE;
        ymin := yc + ( real_max( ritv ) * SIN( a ) );
        ymin_in := a_in AND max_included( ritv );
      ELSE
        ymin_exists := FALSE;
      END_IF;
    END_IF;
    nearest_good_direction( 0.5 * PI, aitv, a, a_in );
    IF SIN( a ) <= 0 THEN
      ymax_exists := TRUE;
      ymax := yc + ( real_min( ritv ) * SIN( a ) );
      ymax_in := a_in AND ( min_included( ritv ) OR ( SIN( a ) = 0 ) );
    ELSE
      IF max_exists( ritv ) THEN
        ymax_exists := TRUE;
        ymax := yc + ( real_max( ritv ) * SIN( a ) );
        ymax_in := a_in AND max_included( ritv );
      ELSE
        ymax_exists := FALSE;
      END_IF;
    END_IF;
    IF NOT ( xmin_exists OR xmax_exists OR ymin_exists OR ymax_exists ) THEN
      RETURN( ? );
    END_IF;
    IF xmin_exists THEN
      IF xmin_in THEN
        min_clo := closed;
      ELSE
        min_clo := open;
      END_IF;
      IF xmax_exists THEN
        IF xmax_in THEN
          max_clo := closed;
        ELSE
          max_clo := open;
        END_IF;
        xitv := make_finite_real_interval( xmin, min_clo, xmax, max_clo );
      ELSE
        xitv := make_real_interval_from_min( xmin, min_clo );
      END_IF;
    ELSE
      IF xmax_exists THEN
        IF xmax_in THEN
          max_clo := closed;
        ELSE
          max_clo := open;
        END_IF;
        xitv := make_real_interval_to_max( xmax, max_clo );
      ELSE
        xitv := the_reals;
      END_IF;
    END_IF;
    IF ymin_exists THEN
      IF ymin_in THEN
        min_clo := closed;
      ELSE
        min_clo := open;
      END_IF;
      IF ymax_exists THEN
        IF ymax_in THEN
          max_clo := closed;
        ELSE
          max_clo := open;
        END_IF;
        yitv := make_finite_real_interval( ymin, min_clo, ymax, max_clo );
      ELSE
        yitv := make_real_interval_from_min( ymin, min_clo );
      END_IF;
    ELSE
      IF ymax_exists THEN
        IF ymax_in THEN
          max_clo := closed;
        ELSE
          max_clo := open;
        END_IF;
        yitv := make_real_interval_to_max( ymax, max_clo );
      ELSE
        yitv := the_reals;
      END_IF;
    END_IF;
    RETURN( make_cartesian_complex_number_region( xitv, yitv ) );

  END_FUNCTION; -- enclose_pregion_in_cregion (line:39578 file:ap242ed2_mim_lf_v1.101_TY.exp)

```

*/

  public static 
  func ENCLOSE_PREGION_IN_CREGION( _ PRGN: ePOLAR_COMPLEX_NUMBER_REGION.PRef?  ) 
    -> eCARTESIAN_COMPLEX_NUMBER_REGION.PRef? {

    //CACHE LOOKUP
    let _params = SDAI.ParameterList( PRGN )
    if case .available(let _cached_value) = _enclose_pregion_in_cregion__cache.cachedValue(params: _params) {
      return _cached_value as? eCARTESIAN_COMPLEX_NUMBER_REGION.PRef
    }

    //PARAMETER VALUES
    var PRGN = PRGN; SDAI.TOUCH(var: &PRGN)

    //NESTED PROCEDURE
    func NEAREST_GOOD_DIRECTION(
      ACART: SDAI.REAL? , 
      AITV: eFINITE_REAL_INTERVAL.PRef? , 
      A: inout SDAI.REAL? , 
      A_IN: inout SDAI.BOOLEAN? 
    ) {

      //PARAMETER VALUES
      var ACART = ACART; SDAI.TOUCH(var: &ACART)
      var AITV = AITV; SDAI.TOUCH(var: &AITV)

      //BODY
      A = ACART
      A_IN = SDAI.BOOLEAN(SDAI.TRUE)
      //
      let _TEMP1 /* SDAI.REAL? */ = AITV?.MIN
      let _TEMP2 /* SDAI.LOGICAL */ = A < _TEMP1
      if SDAI.IS_TRUE( _TEMP2 ) {
        //
        let _TEMP3 /* SDAI.REAL */ = SDAI.REAL(2.0000000000e+00) * SDAI.REAL(SDAI.PI)
        let _TEMP4 /* SDAI.REAL? */ = A + SDAI.FORCE_OPTIONAL(_TEMP3)
        let _TEMP5 /* SDAI.REAL? */ = AITV?.MAX
        let _TEMP6 /* SDAI.LOGICAL */ = _TEMP4 < _TEMP5
        if SDAI.IS_TRUE( _TEMP6 ) {
          return
        }
        //
        let _TEMP7 /* SDAI.REAL */ = SDAI.REAL(2.0000000000e+00) * SDAI.REAL(SDAI.PI)
        let _TEMP8 /* SDAI.REAL? */ = A + SDAI.FORCE_OPTIONAL(_TEMP7)
        let _TEMP9 /* SDAI.REAL? */ = AITV?.MAX
        let _TEMP10 /* SDAI.LOGICAL */ = _TEMP8 .==. _TEMP9
        if SDAI.IS_TRUE( _TEMP10 ) {
          //
          let _TEMP11 /* SDAI.BOOLEAN? */ = MAX_INCLUDED( eMATHS_SPACE.PRef(/*eFINITE_REAL_INTERVAL.PRef*/
              AITV) )
          A_IN = _TEMP11
          return
        }
      }
      else {
        //
        let _TEMP12 /* SDAI.REAL? */ = AITV?.MIN
        let _TEMP13 /* SDAI.LOGICAL */ = A .==. _TEMP12
        if SDAI.IS_TRUE( _TEMP13 ) {
          //
          let _TEMP14 /* SDAI.BOOLEAN? */ = MIN_INCLUDED( eMATHS_SPACE.PRef(/*eFINITE_REAL_INTERVAL.PRef*/
              AITV) )
          A_IN = _TEMP14
          return
        }
        else {
          //
          let _TEMP15 /* SDAI.REAL? */ = AITV?.MAX
          let _TEMP16 /* SDAI.LOGICAL */ = A < _TEMP15
          if SDAI.IS_TRUE( _TEMP16 ) {
            return
          }
          else {
            //
            let _TEMP17 /* SDAI.REAL? */ = AITV?.MAX
            let _TEMP18 /* SDAI.LOGICAL */ = A .==. _TEMP17
            if SDAI.IS_TRUE( _TEMP18 ) {
              //
              let _TEMP19 /* SDAI.BOOLEAN? */ = MAX_INCLUDED( eMATHS_SPACE.PRef(/*eFINITE_REAL_INTERVAL.PRef*/
                  AITV) )
              A_IN = _TEMP19
              return
            }
          }
        }
      }
      //
      let _TEMP20 /* SDAI.REAL? */ = AITV?.MAX
      let _TEMP21 /* SDAI.REAL? */ = ACART - _TEMP20
      let _TEMP22 /* SDAI.REAL? */ = SDAI.COS( _TEMP21 )
      let _TEMP23 /* SDAI.REAL? */ = AITV?.MIN
      let _TEMP24 /* SDAI.REAL? */ = ACART - _TEMP23
      let _TEMP25 /* SDAI.REAL? */ = SDAI.COS( _TEMP24 )
      let _TEMP26 /* SDAI.LOGICAL */ = _TEMP22 >= _TEMP25
      if SDAI.IS_TRUE( _TEMP26 ) {
        //
        let _TEMP27 /* SDAI.REAL? */ = AITV?.MAX
        A = _TEMP27
        //
        let _TEMP28 /* SDAI.BOOLEAN? */ = MAX_INCLUDED( eMATHS_SPACE.PRef(/*eFINITE_REAL_INTERVAL.PRef*/AITV) )
        A_IN = _TEMP28
      }
      else {
        //
        let _TEMP29 /* SDAI.REAL? */ = AITV?.MIN
        A = _TEMP29
        //
        let _TEMP30 /* SDAI.BOOLEAN? */ = MIN_INCLUDED( eMATHS_SPACE.PRef(/*eFINITE_REAL_INTERVAL.PRef*/AITV) )
        A_IN = _TEMP30
      }
    } //END PROCEDURE NEAREST_GOOD_DIRECTION

    //LOCAL
    var XC: SDAI.REAL?  = SDAI.REAL(0); SDAI.TOUCH(var: &XC)
    var YC: SDAI.REAL?  = SDAI.REAL(0); SDAI.TOUCH(var: &YC)
    var XMIN: SDAI.REAL?  = SDAI.REAL(0); SDAI.TOUCH(var: &XMIN)
    var XMAX: SDAI.REAL?  = SDAI.REAL(0); SDAI.TOUCH(var: &XMAX)
    var YMIN: SDAI.REAL?  = SDAI.REAL(0); SDAI.TOUCH(var: &YMIN)
    var YMAX: SDAI.REAL?  = SDAI.REAL(0); SDAI.TOUCH(var: &YMAX)
    var RITV: sREAL_INTERVAL? 
    var XITV: sREAL_INTERVAL? 
    var YITV: sREAL_INTERVAL? 
    var AITV: eFINITE_REAL_INTERVAL.PRef? 
    var XMIN_EXISTS: SDAI.BOOLEAN? 
    var XMAX_EXISTS: SDAI.BOOLEAN? 
    var YMIN_EXISTS: SDAI.BOOLEAN? 
    var YMAX_EXISTS: SDAI.BOOLEAN? 
    var XMIN_IN: SDAI.BOOLEAN?  = SDAI.BOOLEAN(SDAI.FALSE); SDAI.TOUCH(var: &XMIN_IN)
    var XMAX_IN: SDAI.BOOLEAN?  = SDAI.BOOLEAN(SDAI.FALSE); SDAI.TOUCH(var: &XMAX_IN)
    var YMIN_IN: SDAI.BOOLEAN?  = SDAI.BOOLEAN(SDAI.FALSE); SDAI.TOUCH(var: &YMIN_IN)
    var YMAX_IN: SDAI.BOOLEAN?  = SDAI.BOOLEAN(SDAI.FALSE); SDAI.TOUCH(var: &YMAX_IN)
    var A: SDAI.REAL?  = SDAI.REAL(0); SDAI.TOUCH(var: &A)
    var R: SDAI.REAL?  = SDAI.REAL(0); SDAI.TOUCH(var: &R)
    var A_IN: SDAI.BOOLEAN?  = SDAI.BOOLEAN(SDAI.FALSE); SDAI.TOUCH(var: &A_IN)
    var MIN_CLO: nOPEN_CLOSED?  = OPEN; SDAI.TOUCH(var: &MIN_CLO)
    var MAX_CLO: nOPEN_CLOSED?  = OPEN; SDAI.TOUCH(var: &MAX_CLO)
    //END_LOCAL

    //BODY
    //
    let _TEMP1 /* SDAI.BOOLEAN */ = SDAI.EXISTS( PRGN )
    let _TEMP2 /* SDAI.LOGICAL */ =  !_TEMP1
    if SDAI.IS_TRUE( _TEMP2 ) {
      return _enclose_pregion_in_cregion__cache.updateCache(params: _params, value: (nil as 
        eCARTESIAN_COMPLEX_NUMBER_REGION.PRef?))
    }
    //
    let _TEMP3 /* eCOMPLEX_NUMBER_LITERAL.PRef? */ = PRGN?.CENTRE
    let _TEMP4 /* SDAI.REAL? */ = _TEMP3?.REAL_PART
    XC = _TEMP4
    //
    let _TEMP5 /* eCOMPLEX_NUMBER_LITERAL.PRef? */ = PRGN?.CENTRE
    let _TEMP6 /* SDAI.REAL? */ = _TEMP5?.IMAG_PART
    YC = _TEMP6
    //
    let _TEMP7 /* sREAL_INTERVAL? */ = PRGN?.DISTANCE_CONSTRAINT
    RITV = _TEMP7
    //
    let _TEMP8 /* eFINITE_REAL_INTERVAL.PRef? */ = PRGN?.DIRECTION_CONSTRAINT
    AITV = _TEMP8
    NEAREST_GOOD_DIRECTION( ACART: SDAI.REAL(SDAI.PI), AITV: AITV, A: &A, 
      A_IN: &A_IN )
    //
    let _TEMP9 /* SDAI.REAL? */ = SDAI.COS( A )
    let _TEMP10 /* SDAI.LOGICAL */ = _TEMP9 >= SDAI.FORCE_OPTIONAL(SDAI.INTEGER(0))
    if SDAI.IS_TRUE( _TEMP10 ) {
      XMIN_EXISTS = SDAI.BOOLEAN(SDAI.TRUE)
      //
      let _TEMP11 /* SDAI.REAL? */ = REAL_MIN( eMATHS_SPACE.PRef(/*sREAL_INTERVAL*/RITV) )
      let _TEMP12 /* SDAI.REAL? */ = SDAI.COS( A )
      let _TEMP13 /* SDAI.REAL? */ = _TEMP11 * _TEMP12
      let _TEMP14 /* SDAI.REAL? */ = XC + _TEMP13
      XMIN = _TEMP14
      //
      let _TEMP15 /* SDAI.BOOLEAN? */ = MIN_INCLUDED( eMATHS_SPACE.PRef(/*sREAL_INTERVAL*/RITV) )
      let _TEMP16 /* SDAI.REAL? */ = SDAI.COS( A )
      let _TEMP17 /* SDAI.LOGICAL */ = _TEMP16 .==. SDAI.FORCE_OPTIONAL(SDAI.INTEGER(0))
      let _TEMP18 /* SDAI.LOGICAL */ = _TEMP15 || _TEMP17
      let _TEMP19 /* SDAI.LOGICAL */ = A_IN && _TEMP18
      XMIN_IN = SDAI.BOOLEAN(/*SDAI.LOGICAL*/_TEMP19)
    }
    else {
      //
      let _TEMP20 /* SDAI.BOOLEAN? */ = MAX_EXISTS( eMATHS_SPACE.PRef(/*sREAL_INTERVAL*/RITV) )
      if SDAI.IS_TRUE( _TEMP20 ) {
        XMIN_EXISTS = SDAI.BOOLEAN(SDAI.TRUE)
        //
        let _TEMP21 /* SDAI.REAL? */ = REAL_MAX( eMATHS_SPACE.PRef(/*sREAL_INTERVAL*/RITV) )
        let _TEMP22 /* SDAI.REAL? */ = SDAI.COS( A )
        let _TEMP23 /* SDAI.REAL? */ = _TEMP21 * _TEMP22
        let _TEMP24 /* SDAI.REAL? */ = XC + _TEMP23
        XMIN = _TEMP24
        //
        let _TEMP25 /* SDAI.BOOLEAN? */ = MAX_INCLUDED( eMATHS_SPACE.PRef(/*sREAL_INTERVAL*/RITV) )
        let _TEMP26 /* SDAI.LOGICAL */ = A_IN && _TEMP25
        XMIN_IN = SDAI.BOOLEAN(/*SDAI.LOGICAL*/_TEMP26)
      }
      else {
        XMIN_EXISTS = SDAI.BOOLEAN(SDAI.FALSE)
      }
    }
    NEAREST_GOOD_DIRECTION( ACART: SDAI.REAL(0), AITV: AITV, A: &A, A_IN: &A_IN )
    //
    let _TEMP27 /* SDAI.REAL? */ = SDAI.COS( A )
    let _TEMP28 /* SDAI.LOGICAL */ = _TEMP27 <= SDAI.FORCE_OPTIONAL(SDAI.INTEGER(0))
    if SDAI.IS_TRUE( _TEMP28 ) {
      XMAX_EXISTS = SDAI.BOOLEAN(SDAI.TRUE)
      //
      let _TEMP29 /* SDAI.REAL? */ = REAL_MIN( eMATHS_SPACE.PRef(/*sREAL_INTERVAL*/RITV) )
      let _TEMP30 /* SDAI.REAL? */ = SDAI.COS( A )
      let _TEMP31 /* SDAI.REAL? */ = _TEMP29 * _TEMP30
      let _TEMP32 /* SDAI.REAL? */ = XC + _TEMP31
      XMAX = _TEMP32
      //
      let _TEMP33 /* SDAI.BOOLEAN? */ = MIN_INCLUDED( eMATHS_SPACE.PRef(/*sREAL_INTERVAL*/RITV) )
      let _TEMP34 /* SDAI.REAL? */ = SDAI.COS( A )
      let _TEMP35 /* SDAI.LOGICAL */ = _TEMP34 .==. SDAI.FORCE_OPTIONAL(SDAI.INTEGER(0))
      let _TEMP36 /* SDAI.LOGICAL */ = _TEMP33 || _TEMP35
      let _TEMP37 /* SDAI.LOGICAL */ = A_IN && _TEMP36
      XMAX_IN = SDAI.BOOLEAN(/*SDAI.LOGICAL*/_TEMP37)
    }
    else {
      //
      let _TEMP38 /* SDAI.BOOLEAN? */ = MAX_EXISTS( eMATHS_SPACE.PRef(/*sREAL_INTERVAL*/RITV) )
      if SDAI.IS_TRUE( _TEMP38 ) {
        XMAX_EXISTS = SDAI.BOOLEAN(SDAI.TRUE)
        //
        let _TEMP39 /* SDAI.REAL? */ = REAL_MAX( eMATHS_SPACE.PRef(/*sREAL_INTERVAL*/RITV) )
        let _TEMP40 /* SDAI.REAL? */ = SDAI.COS( A )
        let _TEMP41 /* SDAI.REAL? */ = _TEMP39 * _TEMP40
        let _TEMP42 /* SDAI.REAL? */ = XC + _TEMP41
        XMAX = _TEMP42
        //
        let _TEMP43 /* SDAI.BOOLEAN? */ = MAX_INCLUDED( eMATHS_SPACE.PRef(/*sREAL_INTERVAL*/RITV) )
        let _TEMP44 /* SDAI.LOGICAL */ = A_IN && _TEMP43
        XMAX_IN = SDAI.BOOLEAN(/*SDAI.LOGICAL*/_TEMP44)
      }
      else {
        XMAX_EXISTS = SDAI.BOOLEAN(SDAI.FALSE)
      }
    }
    NEAREST_GOOD_DIRECTION( ACART: SDAI.REAL( -5.0000000000e-01) * SDAI.REAL(SDAI.PI), 
      AITV: AITV, A: &A, A_IN: &A_IN )
    //
    let _TEMP45 /* SDAI.REAL? */ = SDAI.SIN( A )
    let _TEMP46 /* SDAI.LOGICAL */ = _TEMP45 >= SDAI.FORCE_OPTIONAL(SDAI.INTEGER(0))
    if SDAI.IS_TRUE( _TEMP46 ) {
      YMIN_EXISTS = SDAI.BOOLEAN(SDAI.TRUE)
      //
      let _TEMP47 /* SDAI.REAL? */ = REAL_MIN( eMATHS_SPACE.PRef(/*sREAL_INTERVAL*/RITV) )
      let _TEMP48 /* SDAI.REAL? */ = SDAI.SIN( A )
      let _TEMP49 /* SDAI.REAL? */ = _TEMP47 * _TEMP48
      let _TEMP50 /* SDAI.REAL? */ = YC + _TEMP49
      YMIN = _TEMP50
      //
      let _TEMP51 /* SDAI.BOOLEAN? */ = MIN_INCLUDED( eMATHS_SPACE.PRef(/*sREAL_INTERVAL*/RITV) )
      let _TEMP52 /* SDAI.REAL? */ = SDAI.SIN( A )
      let _TEMP53 /* SDAI.LOGICAL */ = _TEMP52 .==. SDAI.FORCE_OPTIONAL(SDAI.INTEGER(0))
      let _TEMP54 /* SDAI.LOGICAL */ = _TEMP51 || _TEMP53
      let _TEMP55 /* SDAI.LOGICAL */ = A_IN && _TEMP54
      YMIN_IN = SDAI.BOOLEAN(/*SDAI.LOGICAL*/_TEMP55)
    }
    else {
      //
      let _TEMP56 /* SDAI.BOOLEAN? */ = MAX_EXISTS( eMATHS_SPACE.PRef(/*sREAL_INTERVAL*/RITV) )
      if SDAI.IS_TRUE( _TEMP56 ) {
        YMIN_EXISTS = SDAI.BOOLEAN(SDAI.TRUE)
        //
        let _TEMP57 /* SDAI.REAL? */ = REAL_MAX( eMATHS_SPACE.PRef(/*sREAL_INTERVAL*/RITV) )
        let _TEMP58 /* SDAI.REAL? */ = SDAI.SIN( A )
        let _TEMP59 /* SDAI.REAL? */ = _TEMP57 * _TEMP58
        let _TEMP60 /* SDAI.REAL? */ = YC + _TEMP59
        YMIN = _TEMP60
        //
        let _TEMP61 /* SDAI.BOOLEAN? */ = MAX_INCLUDED( eMATHS_SPACE.PRef(/*sREAL_INTERVAL*/RITV) )
        let _TEMP62 /* SDAI.LOGICAL */ = A_IN && _TEMP61
        YMIN_IN = SDAI.BOOLEAN(/*SDAI.LOGICAL*/_TEMP62)
      }
      else {
        YMIN_EXISTS = SDAI.BOOLEAN(SDAI.FALSE)
      }
    }
    NEAREST_GOOD_DIRECTION( ACART: SDAI.REAL(5.0000000000e-01) * SDAI.REAL(SDAI.PI), 
      AITV: AITV, A: &A, A_IN: &A_IN )
    //
    let _TEMP63 /* SDAI.REAL? */ = SDAI.SIN( A )
    let _TEMP64 /* SDAI.LOGICAL */ = _TEMP63 <= SDAI.FORCE_OPTIONAL(SDAI.INTEGER(0))
    if SDAI.IS_TRUE( _TEMP64 ) {
      YMAX_EXISTS = SDAI.BOOLEAN(SDAI.TRUE)
      //
      let _TEMP65 /* SDAI.REAL? */ = REAL_MIN( eMATHS_SPACE.PRef(/*sREAL_INTERVAL*/RITV) )
      let _TEMP66 /* SDAI.REAL? */ = SDAI.SIN( A )
      let _TEMP67 /* SDAI.REAL? */ = _TEMP65 * _TEMP66
      let _TEMP68 /* SDAI.REAL? */ = YC + _TEMP67
      YMAX = _TEMP68
      //
      let _TEMP69 /* SDAI.BOOLEAN? */ = MIN_INCLUDED( eMATHS_SPACE.PRef(/*sREAL_INTERVAL*/RITV) )
      let _TEMP70 /* SDAI.REAL? */ = SDAI.SIN( A )
      let _TEMP71 /* SDAI.LOGICAL */ = _TEMP70 .==. SDAI.FORCE_OPTIONAL(SDAI.INTEGER(0))
      let _TEMP72 /* SDAI.LOGICAL */ = _TEMP69 || _TEMP71
      let _TEMP73 /* SDAI.LOGICAL */ = A_IN && _TEMP72
      YMAX_IN = SDAI.BOOLEAN(/*SDAI.LOGICAL*/_TEMP73)
    }
    else {
      //
      let _TEMP74 /* SDAI.BOOLEAN? */ = MAX_EXISTS( eMATHS_SPACE.PRef(/*sREAL_INTERVAL*/RITV) )
      if SDAI.IS_TRUE( _TEMP74 ) {
        YMAX_EXISTS = SDAI.BOOLEAN(SDAI.TRUE)
        //
        let _TEMP75 /* SDAI.REAL? */ = REAL_MAX( eMATHS_SPACE.PRef(/*sREAL_INTERVAL*/RITV) )
        let _TEMP76 /* SDAI.REAL? */ = SDAI.SIN( A )
        let _TEMP77 /* SDAI.REAL? */ = _TEMP75 * _TEMP76
        let _TEMP78 /* SDAI.REAL? */ = YC + _TEMP77
        YMAX = _TEMP78
        //
        let _TEMP79 /* SDAI.BOOLEAN? */ = MAX_INCLUDED( eMATHS_SPACE.PRef(/*sREAL_INTERVAL*/RITV) )
        let _TEMP80 /* SDAI.LOGICAL */ = A_IN && _TEMP79
        YMAX_IN = SDAI.BOOLEAN(/*SDAI.LOGICAL*/_TEMP80)
      }
      else {
        YMAX_EXISTS = SDAI.BOOLEAN(SDAI.FALSE)
      }
    }
    //
    let _TEMP81 /* SDAI.LOGICAL */ = XMIN_EXISTS || XMAX_EXISTS
    let _TEMP82 /* SDAI.LOGICAL */ = _TEMP81 || YMIN_EXISTS
    let _TEMP83 /* SDAI.LOGICAL */ = _TEMP82 || YMAX_EXISTS
    let _TEMP84 /* SDAI.LOGICAL */ =  !_TEMP83
    if SDAI.IS_TRUE( _TEMP84 ) {
      return _enclose_pregion_in_cregion__cache.updateCache(params: _params, value: (nil as 
        eCARTESIAN_COMPLEX_NUMBER_REGION.PRef?))
    }
    if SDAI.IS_TRUE( XMIN_EXISTS ) {
      if SDAI.IS_TRUE( XMIN_IN ) {
        MIN_CLO = CLOSED
      }
      else {
        MIN_CLO = OPEN
      }
      if SDAI.IS_TRUE( XMAX_EXISTS ) {
        if SDAI.IS_TRUE( XMAX_IN ) {
          MAX_CLO = CLOSED
        }
        else {
          MAX_CLO = OPEN
        }
        //
        let _TEMP85 /* eFINITE_REAL_INTERVAL.PRef? */ = 
            MAKE_FINITE_REAL_INTERVAL( 
            MIN: XMIN, 
            MINCLO: MIN_CLO, 
            MAX: XMAX, 
            MAXCLO: MAX_CLO )
        XITV = sREAL_INTERVAL(/*eFINITE_REAL_INTERVAL.PRef*/_TEMP85)
      }
      else {
        //
        let _TEMP86 /* eREAL_INTERVAL_FROM_MIN.PRef? */ = MAKE_REAL_INTERVAL_FROM_MIN( 
            MIN: XMIN, 
            MINCLO: MIN_CLO )
        XITV = sREAL_INTERVAL(/*eREAL_INTERVAL_FROM_MIN.PRef*/_TEMP86)
      }
    }
    else {
      if SDAI.IS_TRUE( XMAX_EXISTS ) {
        if SDAI.IS_TRUE( XMAX_IN ) {
          MAX_CLO = CLOSED
        }
        else {
          MAX_CLO = OPEN
        }
        //
        let _TEMP87 /* eREAL_INTERVAL_TO_MAX.PRef? */ = MAKE_REAL_INTERVAL_TO_MAX( 
            MAX: XMAX, 
            MAXCLO: MAX_CLO )
        XITV = sREAL_INTERVAL(/*eREAL_INTERVAL_TO_MAX.PRef*/_TEMP87)
      }
      else {
        XITV = sREAL_INTERVAL(/*eELEMENTARY_SPACE.PRef*/THE_REALS)
      }
    }
    if SDAI.IS_TRUE( YMIN_EXISTS ) {
      if SDAI.IS_TRUE( YMIN_IN ) {
        MIN_CLO = CLOSED
      }
      else {
        MIN_CLO = OPEN
      }
      if SDAI.IS_TRUE( YMAX_EXISTS ) {
        if SDAI.IS_TRUE( YMAX_IN ) {
          MAX_CLO = CLOSED
        }
        else {
          MAX_CLO = OPEN
        }
        //
        let _TEMP88 /* eFINITE_REAL_INTERVAL.PRef? */ = 
            MAKE_FINITE_REAL_INTERVAL( 
            MIN: YMIN, 
            MINCLO: MIN_CLO, 
            MAX: YMAX, 
            MAXCLO: MAX_CLO )
        YITV = sREAL_INTERVAL(/*eFINITE_REAL_INTERVAL.PRef*/_TEMP88)
      }
      else {
        //
        let _TEMP89 /* eREAL_INTERVAL_FROM_MIN.PRef? */ = MAKE_REAL_INTERVAL_FROM_MIN( 
            MIN: YMIN, 
            MINCLO: MIN_CLO )
        YITV = sREAL_INTERVAL(/*eREAL_INTERVAL_FROM_MIN.PRef*/_TEMP89)
      }
    }
    else {
      if SDAI.IS_TRUE( YMAX_EXISTS ) {
        if SDAI.IS_TRUE( YMAX_IN ) {
          MAX_CLO = CLOSED
        }
        else {
          MAX_CLO = OPEN
        }
        //
        let _TEMP90 /* eREAL_INTERVAL_TO_MAX.PRef? */ = MAKE_REAL_INTERVAL_TO_MAX( 
            MAX: YMAX, 
            MAXCLO: MAX_CLO )
        YITV = sREAL_INTERVAL(/*eREAL_INTERVAL_TO_MAX.PRef*/_TEMP90)
      }
      else {
        YITV = sREAL_INTERVAL(/*eELEMENTARY_SPACE.PRef*/THE_REALS)
      }
    }
    //
    let _TEMP91 /* eCARTESIAN_COMPLEX_NUMBER_REGION.PRef? */ = MAKE_CARTESIAN_COMPLEX_NUMBER_REGION( 
        REAL_CONSTRAINT: XITV, 
        IMAG_CONSTRAINT: YITV )
    return _enclose_pregion_in_cregion__cache.updateCache(params: _params, value: _TEMP91)
  }

}

//MARK: - function result cache
private let _enclose_pregion_in_cregion__cache = SDAI.FunctionResultCache(
  label: "ENCLOSE_PREGION_IN_CREGION",
  controller: AP242_MANAGED_MODEL_BASED_3D_ENGINEERING_MIM_LF.schemaDefinition )

